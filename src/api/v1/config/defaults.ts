export const PORT = process.env.PORT || 3000;

export const API_URL = "/api/v1";

export const START_TRAINING_MESSAGE = 'A training is starting! Please make sure to join the training center now! (This is a test of the /training endpoint for my API)';

export const RATE_LIMIT = 10; // Amount of requests a client can send per minute